<Window x:Class="NoteAppView.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NoteAppView"
        mc:Ignorable="d"
        Title="Авторизация" Height="450" Width="800">
    <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                Background="LightGray">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30*"/>
                <ColumnDefinition Width="70*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Grid.Row="0">Login</Label>
            <TextBox x:Name="emailTextBox" Grid.Column="1" Grid.Row="0"
                     >login</TextBox>
            <Label Grid.Column="0" Grid.Row="1">Pass</Label>
            <TextBox x:Name="passTextBox" Grid.Column="1" Grid.Row="1">pass</TextBox>
        </Grid>
        <Button VerticalAlignment="Bottom" Click="AuthBtnClicked">Auth!</Button>
    </StackPanel>
</Window>
